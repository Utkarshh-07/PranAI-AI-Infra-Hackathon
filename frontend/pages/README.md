This folder exports top-level screen wrappers that point to the original `prana/lib/screens` implementation.

Nested screen directories (e.g., `ai_chat`, `auth`, `chat`, `mindfulness`, `parent`, `prankster_flow`, `profile`, `therapist`) should be refactored when needed — keep originals in `prana/lib/screens/` until imports are updated.
