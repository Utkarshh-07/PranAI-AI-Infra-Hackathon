This folder exports top-level widget wrappers that point to the original `prana/lib/widgets` implementation.

Nested widget directories (e.g., `dashboard_widgets`, `ocean`, `onboarding`) should be refactored when needed — keep originals in `prana/lib/widgets/` until imports are updated.
