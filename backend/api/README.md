# API

Place HTTP handlers, controllers, and endpoint entry points here.

Suggested initial files:
- `health_check.dart` — simple status endpoint for verification
- `auth_routes.dart` — authentication-related routes
